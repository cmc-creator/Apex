import { NextResponse } from 'next/server'; import { userFromSession } from '@/lib/server/auth'; export async function GET() { try { const { user } = await userFromSession(); return NextResponse.json({ user }); } catch { return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); } }
